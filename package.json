{
  "name": "web-rpi-universal-remote",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@material/slider": "^0.2.7",
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-scripts": "1.0.14"
  },
  "scripts": {
    "build": "REACT_APP_SERVER_URL=http://localhost:3001 react-scripts build",
    "start:dev": "REACT_APP_SERVER_URL=http://localhost:3001 react-scripts start",
    "start": "forever start app.js",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
